# Конструктор рецептов 107-1/у

Desktop-приложение для подготовки проекта рецепта по форме 107-1/у. Пользователь заполняет форму в графическом интерфейсе, после чего программа подставляет введённые данные в Word-шаблон и сохраняет сформированный `.docx`-файл на Рабочем столе.

**Версия:** 0.2.0  
**Дата сборки:** 29.08.2026  
**Автор:** © 2026 Жихорев Р.С.

> [!IMPORTANT]
> Приложение является вспомогательным инструментом подготовки проекта рецепта. Оно не осуществляет диагностику, не назначает лечение, не определяет дозы, противопоказания или лекарственные взаимодействия.
>
> Перед подписанием, печатью, выдачей или передачей документа медицинский работник обязан самостоятельно проверить все данные пациента, врача, лекарственного назначения, дозировку, способ применения, количество и соответствие документа действующим нормативным требованиям.

## Возможности

- Графическая форма для ввода рецептурных данных.
- Ввод фамилии и инициалов пациента.
- Ввод даты рождения пациента.
- Ввод ФИО врача.
- Ввод лекарственной формы, МНН и дозировки.
- Ввод способа применения препарата.
- Автоматическая подстановка текущих дня, месяца и двух последних цифр года.
- Формирование документа Word `.docx` на основе шаблона.
- Сохранение готового документа на Рабочем столе пользователя.
- Добавление даты и времени формирования в имя файла.
- Отображение версии, даты сборки и информации об авторе в интерфейсе.
- Подготовка к сборке самостоятельного исполняемого файла через PyInstaller.

## Технологии

| Компонент | Назначение |
|---|---|
| Python | Основная логика приложения |
| pywebview | Desktop-окно и взаимодействие между Python и HTML/JavaScript |
| HTML | Разметка формы ввода |
| JavaScript | Сбор данных формы и вызов Python API |
| docxtpl | Заполнение Word-шаблона на основе Jinja-плейсхолдеров |
| PyInstaller | Сборка приложения в исполняемый файл |

## Структура проекта

```text
prescription_constructor/
├── main.py
├── constants.py
├── requirements.txt
├── README.md
├── .gitignore
│
├── templates/
│   └── prescription_107-1u_template.docx
│
└── ui/
    ├── prescription_form.html
    └── form.js
```

### Назначение файлов

| Файл | Назначение |
|---|---|
| `main.py` | Запуск окна приложения, генерация документа `.docx`, сохранение результата |
| `constants.py` | Версия приложения, дата сборки, сведения об авторе |
| `ui/prescription_form.html` | HTML-разметка формы конструктора рецепта |
| `ui/form.js` | Получение данных формы и вызов Python-метода `create_prescription()` |
| `templates/prescription_107-1u_template.docx` | Word-шаблон рецептурного бланка |
| `requirements.txt` | Зафиксированные версии зависимостей |
| `.gitignore` | Исключения для Git: виртуальные окружения, кэш, IDE, `.env`, папки сборки |

## Требования

- Windows 10/11.
- Python 3.10 или новее.
- Microsoft Word либо совместимое приложение для открытия сформированных файлов `.docx`.
- Word-шаблон рецепта в формате `.docx`.

## Установка

### 1. Клонируйте или скачайте проект

```bash
git clone <URL-репозитория>
cd prescription_constructor
```

Если Git не используется, распакуйте архив проекта и перейдите в его корневую папку.

### 2. Создайте виртуальное окружение

```bash
python -m venv .venv
```

### 3. Активируйте окружение

Для `cmd`:

```bat
.venv\Scripts\activate
```

Для PowerShell:

```powershell
.\.venv\Scripts\Activate.ps1
```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
```

Используемые зависимости:

```text
docxtpl==0.20.2
pywebview==6.2.1
pyinstaller==6.22.2
```

## Подготовка Word-шаблона

Шаблон должен находиться по пути:

```text
templates/prescription_107-1u_template.docx
```

В соответствующие поля Word-документа необходимо вставить плейсхолдеры `docxtpl`.

```text
{{ d }}
{{ m }}
{{ y }}

{{ patient_fio }}
{{ patient_birthdate }}
{{ doctor_fio }}

{{ drug_1_form_name_dosage }}
{{ drug_1_signa }}
```

### Переменные шаблона

| Переменная | Подставляемое значение |
|---|---|
| `{{ d }}` | Текущий день месяца |
| `{{ m }}` | Текущий номер месяца |
| `{{ y }}` | Последние две цифры текущего года |
| `{{ patient_fio }}` | Фамилия и инициалы пациента |
| `{{ patient_birthdate }}` | Дата рождения пациента |
| `{{ doctor_fio }}` | ФИО врача |
| `{{ drug_1_form_name_dosage }}` | Лекарственная форма, МНН и дозировка |
| `{{ drug_1_signa }}` | Способ применения с автоматически добавляемым префиксом `S:` |

Пример оформления даты в Word-шаблоне:

```text
«{{ d }}» {{ m }} 20{{ y }} г.
```

> [!WARNING]
> Каждый плейсхолдер должен быть расположен в Word как единый текстовый фрагмент. Не меняйте форматирование внутри выражения `{{ ... }}`: Word может разбить его на несколько фрагментов, из-за чего `docxtpl` не сможет выполнить замену.

## Запуск

В активированном виртуальном окружении выполните:

```bash
python main.py
```

Или укажите интерпретатор виртуального окружения явно:

```bat
.venv\Scripts\python.exe main.py
```

После запуска откроется окно «Конструктор рецептов 107-1/у».

## Работа с приложением

1. Введите фамилию и инициалы пациента.
2. Укажите дату рождения пациента.
3. Введите ФИО врача.
4. Укажите лекарственную форму, МНН и дозировку.
5. Заполните способ применения.
6. Нажмите кнопку «Сформировать рецепт».
7. Проверьте уведомление об успешном сохранении.
8. Откройте созданный `.docx`-файл на Рабочем столе.
9. До подписания или выдачи обязательно проверьте содержание сформированного документа.

## Формирование документа

После отправки формы JavaScript передаёт данные в метод Python:

```python
window.pywebview.api.create_prescription(prescriptionData)
```

Приложение:

1. Получает и очищает введённые значения.
2. Определяет текущую дату и время.
3. Открывает Word-шаблон.
4. Подставляет данные в плейсхолдеры.
5. Сохраняет результат в формате `.docx` на Рабочий стол.

Имя выходного файла формируется по шаблону:

```text
{ФИО пациента} {дата} {время}.docx
```

Пример:

```text
Иванов И.И. 2026-08-23 17-40-15.docx
```

Время записывается в формате `%H-%M-%S`, поскольку символ `:` недопустим в именах файлов Windows.

> [!NOTE]
> Имя файла содержит персональные данные пациента. Для использования в реальной информационной среде рекомендуется рассмотреть переход на внутренний идентификатор пациента или иной вариант, согласованный с локальными правилами обработки персональных данных.

## Настройка метаданных

Версия, дата сборки и текст в нижней части приложения находятся в файле `constants.py`:

```python
APP_VERSION = '0.1.1'
BUILD_DATE = '23.08.2026'
COPYRIGHT = '© 2026 Жихорев Р.С. Конструктор рецептов 107-1/у'
```

После изменения значений интерфейс получает их через метод `get_app_info()`.

## Сборка EXE

Для создания самостоятельного исполняемого файла можно использовать PyInstaller.

Пример команды:

```bash
pyinstaller --noconfirm --onefile --windowed ^
  --add-data "templates;templates" ^
  --add-data "ui;ui" ^
  main.py
```

После сборки исполняемый файл будет создан в папке:

```text
dist/
```

> [!WARNING]
> Перед распространением `.exe` необходимо проверить работу программы на чистой Windows-системе: открытие интерфейса, доступ к шаблону, формирование документа и сохранение на Рабочий стол.

## Ограничения текущей версии

- Поддерживается один лекарственный препарат в сформированном документе.
- Не реализованы автоматические проверки доз, противопоказаний, лекарственных взаимодействий и клинической обоснованности назначения.
- Не реализовано хранение рецептов, пациентов или истории операций.
- Не реализована аутентификация и разграничение прав пользователей.
- Путь сохранения фиксирован на Рабочем столе текущего пользователя.
- При использовании OneDrive путь к Рабочему столу может отличаться от стандартного.
- В имени сформированного файла используется ФИО пациента.
- Требуется наличие корректного Word-шаблона в папке `templates`.

## Типичные ошибки

### Не найден шаблон Word

Если появляется ошибка, связанная с отсутствием файла шаблона, проверьте наличие файла:

```text
templates/prescription_107-1u_template.docx
```

Убедитесь, что:

- имя файла совпадает с указанным в `main.py`;
- файл имеет расширение `.docx`;
- документ не является старым файлом `.doc`, которому вручную изменили расширение;
- при сборке в `.exe` папка `templates` включена в пакет.

### Приложение не запускается

Проверьте:

```bash
python --version
pip show pywebview
pip show docxtpl
```

При необходимости переустановите зависимости:

```bash
pip install --upgrade -r requirements.txt
```

### Документ не появился на Рабочем столе

В текущей версии путь определяется так:

```python
Path.home() / 'Desktop'
```

Если Рабочий стол перенесён в OneDrive или имеет нестандартное расположение, путь может не соответствовать фактической папке. В этом случае следует доработать настройку каталога сохранения либо добавить выбор папки пользователем.

### Плейсхолдеры остались в документе

Вероятная причина — Word разбил выражение вида `{{ patient_fio }}` на несколько текстовых фрагментов из-за форматирования.

Удалите плейсхолдер в шаблоне, вставьте его заново целиком и не изменяйте оформление отдельных символов внутри выражения.

## Безопасность и ответственность

Приложение обрабатывает сведения пациента, поэтому при использовании в медицинской организации следует обеспечить соблюдение внутренних правил информационной безопасности и требований к обработке персональных данных.

Сформированный файл является проектом документа. Пользователь несёт ответственность за:

- достоверность введённых данных;
- корректность данных пациента и врача;
- выбор препарата, лекарственной формы, дозировки и режима применения;
- проверку противопоказаний, взаимодействий и индивидуальных особенностей пациента;
- проверку соответствия рецепта действующим нормативным требованиям;
- проверку документа до его подписания, печати, выдачи или передачи.

Приложение не заменяет клиническое решение врача.